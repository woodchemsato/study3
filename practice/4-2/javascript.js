$(function() {

  // ulで全部取得
  // console.log($('ul')); // ←これだとulそのものしか取れません
  // console.log($('ul').children('li'));

  // 復習　２番目を取得
  // console.log($('ul').children('li').eq(1));

  // 要素の追加系 バリエーション

  // append 指定した要素の子要素に追加
  // $('ul').append('<li id="id4-2_4">id は id4-2_4 です</li>');

  // appendTo 指定した要素の子要素の末尾にモノを指定して追加  
  // $('<li id="id4-2_5">id は id4-2_5 です</li>').appendTo('ul');

  // prepend 指定した要素の子要素の先頭に追加
  // $('ul').prepend('<li id="id4-2_6">id は id4-2_6 です</li>');

  // appendTo 指定した要素の子要素の先頭にモノを指定して追加
  // $('<li id="id4-2_7">id は id4-2_7 です</li>').prependTo('ul');

  // 課題4-2(1)： 
  // idが「#id4-2_2」の要素のあとに以下を追加してください。
  // 「jquery 要素 追加 指定」　でググってください

});