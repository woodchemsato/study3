$(function() {

  // ulで全部取得
  // console.log($('ul').children('li'));

  // 復習　２番目を取得
  // console.log($('ul').children('li').eq(1));

  // 要素の削除系 バリエーション

  // 子要素の最後を指定 remove()
  // $('li:last').remove();

  // 指定したidを削除
  // $('li#id4-3_3').remove();

  // 課題：子要素を全て削除
  // 「jquery 子要素 全部 削除」 でググってください
  // $('ul').empty();
});